version: '1.0'

steps:
    step-name:
      type: build
      description: Maven example
      dockerfile: Dockerfile
      image-name: ncodefresh/ncodefresh/springmvchelloworld
      tag: ${{CF_BRANCH}}